name: drupalorg-cli

project_files:
  - web-build/Dockerfile.drupalorg-cli
  - drupalorg-cli/scripts/configure-completion.php

ddev_version_constraint: '>= v1.24.3'

post_install_actions:
  - |
    <?php
    #ddev-description: Adapt completion install to the project's ssh-shell
    require 'drupalorg-cli/scripts/configure-completion.php';
